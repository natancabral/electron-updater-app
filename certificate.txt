package.json

"build": {
  "win": {
    "certificateFile": "./certificate/certificate.pfx",
    "certificatePassword": "[password]",
  }
}

electron-builder.yml:

win
  certificateFile: ./certificate/certificate.pfx,
  certificatePassword: [password],
